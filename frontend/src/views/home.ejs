<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Multi Showcase</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script src="https://unpkg.com/feather-icons"></script>
  </head>
  <body class="bg-[#0f1533] text-white font-sans">
    <div class="flex mx-auto min-h-screen">
      <!-- Sidebar -->
      <%- include('components/sidebar') %>
  <main class="flex-1 border-x p-2 rounded border-gray-700">
        <!-- Tabs -->
        <div class="flex justify-center border-b border-gray-700">
          <a
            href="/"
            aria-current="<%= activeTab === 'for-you' ? 'page' : 'false' %>"
            class="block w-1/2 py-3 text-center border-b-2 -mb-px transition <%= activeTab === 'for-you' ? 'border-purple-400 text-white' : 'border-transparent text-gray-400 hover:text-white hover:border-purple-400' %>"
          >
            For you
          </a>

          <a
            href="/following"
            aria-current="<%= activeTab === 'following' ? 'page' : 'false' %>"
            class="block w-1/2 py-3 text-center border-b-2 -mb-px transition <%= activeTab === 'following' ? 'border-purple-400 text-white' : 'border-transparent text-gray-400 hover:text-white hover:border-purple-400' %>"
          >
            Following
          </a>
        </div>

        <!-- New Post Box -->
        <%- include('components/newpost', { placeholder: "What's happening?" })
        %>

        <!-- Feed Posts -->
        <div class="p-4 space-y-4">
          <% if (feed && feed.length) { %> <% feed.forEach(post => { %>
          <div data-post-id="<%= post.id %>">
            <%- include('components/post', post) %>
          </div>
          <% }) %> <% } else { %>
          <p class="text-center text-gray-400 py-6">No posts yet.</p>
          <% } %>
        </div>
      </main>
    </div>

    <script>
      feather.replace();
    </script>
  </body>
</html>
