<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Reset Password • Multishowcase</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
        referrerpolicy="no-referrer" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;800&display=swap" rel="stylesheet">
  <style>
    html,body {
      font-family: 'Inter', system-ui, -apple-system, Segoe UI, Roboto, 'Helvetica Neue', Arial, 'Noto Sans', 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol';
    }
  </style>
</head>
<body class="min-h-screen bg-white">
  <div class="grid grid-cols-1 md:grid-cols-2 min-h-screen">
  <section class="bg-[#1f2743] text-white px-8 md:px-12 lg:px-16 py-10 md:py-14 flex flex-col">
      <div class="max-w-xl w-full mx-auto md:ml-0">
        <div class="text-2xl font-extrabold leading-tight">
          <span class="block">Multi</span>
          <span class="block -mt-1">showcase</span>
        </div>
        <div class="mt-20 md:mt-28">
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-extrabold leading-tight">
            Enter your code
          </h1>
          <p class="mt-5 text-slate-300 max-w-md">
            Check your inbox for the verification code and set a new password below.
          </p>
        </div>
        <div class="flex justify-center items-center h-40 mt-20">
          <i class="fa-solid fa-envelope-open-text fa-9x"></i>
        </div>
      </div>
    </section>

    <section class="px-8 md:px-12 lg:px-16 py-12 md:py-16">
      <div class="max-w-md mx-auto mt-20 md:mt-20">
        <h2 class="text-2xl md:text-3xl font-extrabold bg-gradient-to-r from-purple-400 via-pink-500 to-red-500 bg-clip-text text-transparent">
          Reset your password
        </h2>
        <p class="text-slate-500 mt-2">Use the confirmation code from your email to finish resetting your password.</p>

        <% if (typeof message === 'string' && message.length) { %>
          <div class="mt-6 text-sm text-green-600"><%= message %></div>
        <% } %>
        <% if (typeof error === 'string' && error.length) { %>
          <div class="mt-6 text-sm text-red-600"><%= error %></div>
        <% } %>

        <form class="mt-8 space-y-4" method="post" action="/forgot-password/confirm">
          <input type="hidden" name="username" value="<%= username || '' %>">

          <div>
            <label for="code" class="block text-sm text-slate-600 mb-1">Verification code</label>
            <input id="code" name="code" type="text" required
                   class="w-full rounded-md bg-slate-100/70 border border-slate-200 focus:border-slate-400 focus:ring-0 p-3"
                   placeholder="Enter the 6-digit code">
          </div>

          <div>
            <label for="password" class="block text-sm text-slate-600 mb-1">New password</label>
            <input id="password" name="password" type="password" minlength="8" required
                   class="w-full rounded-md bg-slate-100/70 border border-slate-200 focus:border-slate-400 focus:ring-0 p-3"
                   placeholder="Enter a new password">
            <p class="text-xs text-slate-400 mt-1">Must be at least 8 characters</p>
          </div>

          <button type="submit"
                  class="w-full py-3 rounded-full text-white font-semibold shadow-sm bg-gradient-to-r from-slate-800 to-slate-400">
            Reset password
          </button>
        </form>

        <p class="mt-10 text-sm text-slate-600 text-center">
          Remembered your password?
          <a href="/login" class="font-semibold hover:underline">Back to login</a>
        </p>
      </div>
    </section>
  </div>
</body>
</html>
