<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Explore</title>
    <link href="/style.css" rel="stylesheet" />
    <script src="https://unpkg.com/feather-icons"></script>
  </head>
  <body class="bg-[#0f1533] text-white font-sans">
    <div class="flex mx-auto min-h-screen">
      <!-- Sidebar -->
      <%- include('components/sidebar') %>
  <main class="flex-1 border-x p-2 rounded border-gray-700">
        <!-- Search bar -->
        <div class="px-4 pt-4 mt-4">
        <div class="w-full flex items-center justify-center">
            <div class="flex items-center w-full max-w-2xl bg-transparent border-2 border-gray-600 rounded-2xl px-4 py-2">
                <i data-feather="search" class="text-white mr-3" style="width:22px;height:22px;"></i>
                <input
                    type="text"
                    placeholder="Search"
                    class="flex-1 bg-transparent outline-none text-white placeholder-white text-base font-medium"
                />
            </div>
        </div>
        </div>
        <!-- Tabs -->
  <div class="flex px-4 mt-4 border-b border-gray-700 overflow-x-auto">
          <a href="#" class="py-3 px-4 text-center border-b-2 -mb-px transition border-purple-400 text-white font-semibold">For you</a>
          <a href="#" class="py-3 px-4 text-center border-b-2 -mb-px transition border-transparent text-gray-400 hover:text-white hover:border-purple-400">Model</a>
          <a href="#" class="py-3 px-4 text-center border-b-2 -mb-px transition border-transparent text-gray-400 hover:text-white hover:border-purple-400">Animation 3D</a>
          <a href="#" class="py-3 px-4 text-center border-b-2 -mb-px transition border-transparent text-gray-400 hover:text-white hover:border-purple-400">Game Dev</a>
          <a href="#" class="py-3 px-4 text-center border-b-2 -mb-px transition border-transparent text-gray-400 hover:text-white hover:border-purple-400">UX/UI design</a>
          <a href="#" class="py-3 px-4 text-center border-b-2 -mb-px transition border-transparent text-gray-400 hover:text-white hover:border-purple-400">Website</a>
        </div>
        <!-- Feed Posts -->
        <div class="p-4 space-y-4">
          <% if (feed && feed.length) { %>
            <% feed.forEach(post => { %>
              <%- include('components/post', post) %>
            <% }) %>
          <% } else { %>
            <p class="text-center text-gray-400 py-6">No posts yet.</p>
          <% } %>
        </div>
      </main>
    </div>
    <script>
      feather.replace();
    </script>
  </body>
</html>
